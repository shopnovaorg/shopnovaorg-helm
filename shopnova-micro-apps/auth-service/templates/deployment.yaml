{{- /* auth-service templates — Argo Rollouts blue-green */}}

{{- /* ── Active Service (live traffic) ─────────────────────────────────── */}}
apiVersion: v1
kind: Service
metadata:
  name: auth-service-active
  namespace: {{ .Values.namespace }}
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  type: ClusterIP
---
{{- /* ── Preview Service (pre-promotion / test traffic) ───────────────── */}}
apiVersion: v1
kind: Service
metadata:
  name: auth-service-preview
  namespace: {{ .Values.namespace }}
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  type: ClusterIP
---
{{- /* ── Rollout (replaces Deployment) ──────────────────────────────────── */}}
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: auth-service
  namespace: {{ .Values.namespace }}
  labels:
    app: auth-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: auth-service
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.port }}
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: shopnova-config
                  key: AUTH_PORT
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: shopnova-config
                  key: NODE_ENV
            - name: AUTH_MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: shopnova-secrets
                  key: AUTH_MONGO_URI
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: shopnova-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}
            limits:
              cpu: {{ .Values.resources.limits.cpu }}
              memory: {{ .Values.resources.limits.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
  strategy:
    blueGreen:
      activeService: auth-service-active       # receives live traffic
      previewService: auth-service-preview     # receives test traffic before promotion
      autoPromotionEnabled: {{ .Values.rollout.autoPromotion }}
---
{{- /* ── HPA targeting the Rollout ──────────────────────────────────────── */}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-service-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: argoproj.io/v1alpha1          # updated: targets Rollout not Deployment
    kind: Rollout
    name: auth-service
  minReplicas: {{ .Values.hpa.minReplicas }}
  maxReplicas: {{ .Values.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.cpuUtilization }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.memoryUtilization }}
