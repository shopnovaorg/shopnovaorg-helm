# Auth Service â€” dev overrides (dev branch)
namespace: dev

image:
  tag: dev-1bbc24e

hpa:
  maxReplicas: 2

resources:
  requests:
    cpu: 64m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

rollout:
  autoPromotion: true   # dev — auto promote after readiness checks
