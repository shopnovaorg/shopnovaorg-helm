# Auth Service — dev overrides (dev branch)
namespace: dev

image:
  tag: dev-f6e1300

hpa:
  maxReplicas: 2

resources:
  requests:
    cpu: 64m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

rollout:
  autoPromotion: true   # dev � auto promote after readiness checks
