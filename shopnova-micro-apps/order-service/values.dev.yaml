# Order Service — dev overrides (dev branch)
namespace: dev

image:
  tag: latest          # ← helm-updater-dev writes this field

hpa:
  maxReplicas: 2

resources:
  requests:
    cpu: 64m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
