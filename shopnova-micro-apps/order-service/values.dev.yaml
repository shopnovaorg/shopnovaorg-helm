# Order Service — dev overrides (dev branch)
namespace: dev

image:
  tag: dev-fb4603d

hpa:
  maxReplicas: 2

resources:
  requests:
    cpu: 64m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
